*.pprof
*.log
*.db
.DS_Store
.idea/
*.csv
# Keep the bundled sample dataset used by the quick start.
!examples/sample-data/*.csv

# Backtest output directory
/results/

# Compiled command binaries accidentally built into the repo root
/at
/backtesting
/at-ig
/at-coinbase
/import-histdata

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
